[pytest]
minversion = 7.0
testpaths = tests
python_files = test_*.py
addopts = -v --strict-markers
markers =
    unit: fast, isolated tests with no database or network access
    api: tests that exercise API endpoints through FastAPI's TestClient
    e2e: tests that walk through a full user-facing workflow end to end
